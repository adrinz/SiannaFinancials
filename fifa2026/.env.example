# Copy to .env and fill in (never commit .env)

# Voice (pick one)
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=

# YouTube upload (OAuth client secrets JSON path)
YOUTUBE_CLIENT_SECRETS=secrets/client_secret.json
YOUTUBE_TOKEN_PATH=secrets/youtube_token.json

# Optional: stock video API
PEXELS_API_KEY=
